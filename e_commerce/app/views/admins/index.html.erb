<h1 align="center">All Admins List</h1>

  <div align="center">
    <%= will_paginate %>
    <% @admins.each do |admin| %>
      <ul class="listing">
        <div class="row">
          <div class="well col-md-4 col-md-offset-4">
            <li><%= link_to gravatar_for(admin), admin_path(admin) %></li>
              <li class="article-title">
                <%= link_to admin.name, admin_path(admin) %>
              </li>
          </div>
        </div>
      </ul>
    <% end %>
    <%= will_paginate %>
  </div>
